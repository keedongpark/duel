using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Assets.scripts
{
    public class PlayerOpponent : Player
    {

    

        public override void Finish()
        {
            base.Finish();

            // Unsubscribe
        }

        private void OnGameStart(string cmd)
        {
        }

        private void OnDeal(string cmd)
        {
        }

        private void OnStartBettingRound(string cmd)
        {
        }

        private void OnCall(string cmd)
        {
        }

        private void OnEndBettingRound(string cmd)
        {
        }
    }
}
